getData: (args) ->
  # body...
  # returns some Data

extractVariables: (data) ->
  # Returns a VarSet

transformVariables: (VarSet) ->
  # Returns a VarSet
  # TODO ? Might be the same as applyAlgebra

applyAlgebra: (varSet) ->
  # Returns a VarSet

applyScales: (varSet) ->
  # Returns a Graph

computeStatistics: (varSet) ->
  # Returns a Graph

constructGeometry: (graph) ->
  # Returns a Graph

applyCoordinates: (graph) ->
  # Returns a Graph

computeAesthetics: (graph) ->
  # Returns a Graphic

render: (graphic) ->
  #Produces an output.

